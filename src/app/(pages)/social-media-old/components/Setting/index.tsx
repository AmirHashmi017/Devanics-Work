import ProfileIntro from '@/app/(pages)/social-media-old/user/[id]/components/Profile';
import React from 'react';

const Setttings = () => {
  console.log('test');
  return <ProfileIntro isSettings />;
};

export default Setttings;
