import { styled } from "@mui/material/styles";

export const DescriptionError = styled("div")(({ theme }) => ({
  color: theme.palette.error.main,
  margin: "3px 14px 0px",
  fontSize: "0.75rem",
}));
